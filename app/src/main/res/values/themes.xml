<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The whole UI is Compose, so the XML theme only needs to be a sensible
         base for the window (day/night aware, no action bar). -->
    <style name="Theme.SprayDay" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
